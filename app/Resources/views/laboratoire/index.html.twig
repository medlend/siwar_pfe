
{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div id="page-wrapper">

        <div class="row">
            <div class="col-lg-12">
                <h4 class="page-header">Hopitales liste</h4>
            </div>
            <!-- /.col-lg-12 -->
        </div>

        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    {#<div class="panel-heading">#}
                    {#DataTables Advanced Tables#}
                    {#</div>#}
                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <table width="100%" class="table table-striped table-bordered table-hover"
                               id="dataTablesHopitales">
                            <thead>
                            <tr>
                                <th>Nom laboratoire</th>
                                <th>Tel laboratoire</th>
                                <th>Heure d'ouverture</th>
                                <th>heure de fermeture</th>
                                <th>Actions</th>
                            </tr>
                            </thead>
                            <tbody>

                            {% for laboratoire in laboratoires %}
                                <tr>
                                    <td><a href="{{ path('laboratoire_show', { 'id': laboratoire.id }) }}">{{ laboratoire.nomLab  }}</a></td>
                                    <td>{{ laboratoire.telLab  }}</td>
                                    <td>{{ laboratoire.hOuverture }}</td>
                                    <td>{{ laboratoire.hFermeture }}</td>
                                    <td>
                                        {% if laboratoire.user.enabled == 1 %}
                                            <a class="btn btn-outline btn-info"  href="{{ path('laboratoire_deactiver', { 'id': laboratoire.id  }) }}">désactiver</a>
                                        {% elseif laboratoire.user.enabled == 0 %}
                                            <a class="btn btn-outline btn-warning"  href="{{ path('laboratoire_activer', { 'id': laboratoire.id  }) }}">activer</a>
                                        {% endif %}
                                        <a class="btn btn-outline btn-danger"  href="{{ path('laboratoire_delete', { 'id': laboratoire.id  }) }}">supp</a>
                                    </td>
                                </tr>
                            {% endfor %}

                            </tbody>
                        </table>

                    </div>
                    <!-- /.panel-body -->
                </div>
                <!-- /.panel -->
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->

        {#<div class="row">#}

        {#<ul>#}
        {#<li>#}
        {#<a class="btn btn-primary btn-lg btn-block" href="{{ path('hopitale_new') }}">Create a new hopitale</a>#}
        {#</li>#}
        {#</ul>#}
        {#</div>#}
        <!-- /.row -->
        <!-- /.container-fluid -->
    </div>



    <script>

        $(document).ready(function(){
            $('#dataTablesHopitales').DataTable();
        });
    </script>

{% endblock %}